// Print and Return
//
// Your function will receive an array with two numbers. Print the first value, and return the second.

function printReturn(printNum, returnNum) {
  var arr = [printNum, returnNum];
  console.log(arr[0]);
  return arr[1];
}

printReturn(2, 5);
